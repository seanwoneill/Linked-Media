<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aa101.x501.linkedmedia"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="21" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".JabberwockyActivity"
            android:label="@string/title_activity_jabberwocky" >
        </activity>
        <activity
            android:name=".WaroftheWorldsActivity"
            android:label="@string/title_activity_war_of_the_worlds" >
        </activity>
        <activity
            android:name=".RollingBallActivity"
            android:label="@string/title_activity_rolling_ball" >
        </activity>
    </application>

</manifest>
